import React from 'react';
import Navbar from '../shared/navbar/navbar';
import useAuth from '../../hooks/useAuth';

const Home = () => {
   
    return (
        <div>
            Home
        </div>
    );
};

export default Home;